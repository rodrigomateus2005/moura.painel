import { MaterializeModalDirective } from './materialize-modal.directive';

describe('MaterializeModalDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterializeModalDirective();
    expect(directive).toBeTruthy();
  });
});
